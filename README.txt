== JK Drive Amplifier ==

This plugin is an LV2 plugin.
It has audio ports which contain an array of `float`,
and a two control ports, drive and gain.

The plugin does basic wave shaping distortion with oversampling to reduce
inharmonic aliases. It produces mostly odd order hamonic distortion.
